/*document.addEventListener('load', (loadEvent) => {
    let menuBurger = document.querySelector('#myTopnav');
    let burgerBtn = document.querySelector('.menu_burger');

    console.log('les problemes commence!');

    burgerBtn.addEventListener('click', (event) => {
        console.log('les problemes commence!');
        if(menuBurger.style.left === "0px"){
            menuBurger.style.left = "-100vw";
        } else {
            menuBurger.style.left = "0";
        }
    });
});*/